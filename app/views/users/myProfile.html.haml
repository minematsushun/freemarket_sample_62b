
.wrapper.clearfix
  = render 'shared/main_header'
  .breadcrumbs
    .breadcrumbs__line
      - breadcrumb :profile
      = breadcrumbs separator: "#{content_tag(:i, '', class: 'fas fa-chevron-right')}", class: "breadcrumbs-list"
  %main.i-container.clearfix
    .i-content
      %section.i-chapter-container
        %h2.i-chapter-heads
          プロフィール
        = form_with(model: @user, local: true) do |f|
          .setting-profile
            .setting-profile-image
              %img{height: "60", src: "//static.mercdn.net/images/member_photo_noimage_thumb.png", width: "60"}/
            = f.text_field :nickname, placeholder: @user.nickname, class: "input-defaults"
          .setting-profile-contents
            = f.text_area :introduce, placeholder: "例）こんにちは☆ ご覧いただきありがとうございます！かわいいものやきれいめオフィスカジュアル中心に出品しています。服のサイズは主に9号です。気持ちよくお取引できるよう心がけていますので、商品や発送方法などご質問がありましたらお気軽にどうぞ♪", class: "textarea-defaults"
            = f.submit "変更する", class: "submit-btn"
    .side_bar
      =render 'shared/side_bar'
  .footer
    = render 'shared/announcement'
    = render 'shared/main_footer'
